function onCreate()
	for i = 0, getProperty('unspawnNotes.length')-1 do
		if getPropertyFromGroup('unspawnNotes', i, 'noteType') == 'thedudnote' then
			setPropertyFromGroup('unspawnNotes', i, 'texture', 'thedud'); 
		end
	end
end